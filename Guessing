# **Guessing.md**
This is my lab guessing game using mermaid to show it works visually
```mermaid
flowchart TD
  Start([Start]) --> B(Generate a random integer between 1 and 10)
B --> C(Guess  3)
C --> D(If integer) & E(If not integer)
E --> B
D --> F(Higher)
F --> H(Guess 8)
H --> I(correct)
I -->J([End])
```
user starts the game then chooses a integer between 1 to 10. The User choose the 
